/**
 * @deprecated Import `compressImage` from `@/lib/utils/compressImage` instead.
 * Kept as a stable alias for older imports.
 */
export {
  compressImage as resizeImageFileForUpload,
  COMPRESS_MAX_WIDTH_PX as CLIENT_UPLOAD_MAX_WIDTH,
} from "@/lib/utils/compressImage";
