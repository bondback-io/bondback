/** Top bar, guest drawer, FAB, loading text — not the account/avatar menu row. */
export const BROWSE_JOBS_NAV_LABEL = "Browse Jobs";

/** Account menu (avatar) only — emphasizes locality for cleaners. */
export const BROWSE_JOBS_AVATAR_MENU_LABEL = "Browse Jobs Near Me";
