Marketplace / listings / jobs — snapshot before consolidation (2026-03-29)

Contents (mirrors paths under repo root):
  lib/jobs/load-job-for-detail-route.ts
  lib/navigation/listing-or-job-href.ts
  lib/marketplace/*.ts
  lib/actions/jobs-list.ts

To revert: copy files back over the same paths in the project root, then run tests.
Prefer git history (git checkout -- <path> or git revert) when available.

Current canonical imports for new code: @/lib/marketplace
